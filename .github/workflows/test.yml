name: release

permissions:
  contents: write

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: test
        run: |
          powershell ./install.ps1
          Get-ChildItem ./mpv
